# CE3 restart operations - 2026-09-16

**Latest status: requested VM created; overall ARM deployment failed.
Four A100 80GB devices and driver readiness verified. VM stopped/deallocated,
verified at 04:32:32.890 UTC, with disks/logs retained. No scientific model execution.**

Baseline: `63e9f34`. The operator authorized one four-A100 VM in Azure China
East 3 (`chinaeast3`), staged experiment startup, and repository execution records
at `2026-09-16T09:28:46.650+08:00`.

## Observed state

| Item | Observation |
|---|---|
| Operator-reported portal login | Complete, according to the operator |
| Login seen by the automation connection | Successfully verified; later sessions timed out/expired |
| Selected subscription | `d124de35-7837-4ffe-ba2b-2ec1d31477d0`, enabled, Owner, Enterprise Agreement |
| Requested region / size | `chinaeast3` / `Standard_NC96ads_A100_v4` |
| Requested VM | `wda-ce3-a100x4-0916`, `Standard_NC96ads_A100_v4`, now Stopped (deallocated) |
| Creation method | Fixed-input ARM template loaded from the project branch at `a78fd55c715bc4b355fb3ceb1d73f6eec4268357` |
| Overall deployment | Failed; failed component not identified, but VM independently verified Running |
| Live quota / EA price | Not independently verified; this exact VM nevertheless provisioned |
| Preferred control plane | Azure CLI, per the operator |
| Deployment submitted | Once |
| Resources created / model calls | One VM confirmed plus associated resources; total unreconciled / 0 |
| Phase 0 / A / B / C / D | Not started |

Initially the browser canvas handle was not registered when the agent tried to
read it. Reopening the canvas reached the sign-in page. One page read timed out;
subsequent visible-text inspection and a structured page read both showed the
account-entry page. Later, using the operator-authorized temporary login, the
agent reached the portal and verified Owner access on the chosen subscription.
No login credentials, cookies or tokens were placed in repository artifacts.
The "stay signed in" prompt was declined when shown.

The subscription's usage/quota page and later VM navigation did not complete
reliably: structured page reads and navigation timed out, and the session
subsequently returned to sign-in. No deployment was submitted.

The paragraphs above describe the earlier browser-access and selector failures,
not the later fixed-template attempt below.

The previous Azure CLI attempt was denied access to its command-log directory
by the local sandbox. Browser authentication does not repair that filesystem
restriction. No token extraction, credential transfer, or sandbox bypass was
attempted. OAuth URL queries and fragments are intentionally not recorded.

## Evidence and recordkeeping

- [Operator authorization](../runs/operations/20260916-ce3-restart/authorization.json)
- [Ordered operation log](../runs/operations/20260916-ce3-restart/log.jsonl)
- [Current project status](../STATUS.json)

These are **operations records**, not scientific trials or Phase 0 results.
Events without an independently captured wall-clock timestamp identify the
request during which they were observed; they do not invent execution times.
No model outputs or qualification results exist for this attempt.

## Resume conditions

1. Permit Azure CLI to use its normal configuration directory through the
   current sandbox policy. No elevation, token extraction, credential copying,
   log redirection or alternative configuration path is used as a workaround.
   Then run `python tools/ce3_preflight.py --inspect-azure` to read the selected
   subscription, exact CE3 SKU and both family/regional quota.
2. Record the subscription, resource IDs, size, price, access configuration and
   shutdown/runtime controls before submitting a deployment.
3. Repair the instrument, precision, scoring, bootstrap and phase-governance
   blockers identified in the September 5 assessment before model trials.
4. Record approved configuration and seals before qualification; start the
   calibration stage only. Formal confirmation requires both subject
   qualifications and Freeze-2. Provisioning is not qualification.

## Local preparation

- [Prepared ARM template](../infra/azure/ce3-a100x4.json): one VM, no inbound
  allow rules, no password authentication, explicit image version/public key/
  shutdown time required. No model-start cloud-init or deployment was invoked.
- [CLI preflight](../tools/ce3_preflight.py): default mode prints a plan without
  calling Azure. Explicit inspection is read-only, bound to the selected
  subscription and CE3; a failing command is never retried.
- Paired bootstrap and signed integer scoring were repaired and tested with
  synthetic data. Nonfinite replicates and missing paired cells are rejected.
- Subspace no-ops preserve precision, and native-template/exposure receipts
  distinguish actual processed positions from token caps.
- Governance now requires mandatory artifacts and immutable measured receipts;
  C/D remain blocked by the unresolved scientific direction provider.
- A toy-tested model adapter exists, but real reference-lens and positive-control
  qualification and a deployable, timed calibration runner remain outstanding.

The old `FREEZE-1.json` has not been modified or silently regenerated. It is a
historical schema-1 candidate, not valid for the repaired code. Schema-2
verification refuses it. A new scientific freeze still requires the dependency
lock, upstream/calibration provenance, approved configuration and resolution of
the documented instrument questions.

All local tests and synthetic fixtures are engineering activity only. No
confirmation bank or model-facing scientific result was produced by them.

## Engineering validation record

The final combined offline suite passed **483 tests**, with no failures,
errors or skips. This includes a CPU toy identity-hook test; no registered
checkpoint was loaded.

The first combined run had six test-helper import failures (477 passed).
Pytest fixture injection corrected the ambiguous `conftest` import; both
the failed and corrected reports are retained:

- [Initial combined test report](../runs/operations/20260916-ce3-restart/engineering-tests.xml)
- [Final combined test report](../runs/operations/20260916-ce3-restart/engineering-tests-final.xml)
- [Commands, hashes and scope](../runs/operations/20260916-ce3-restart/engineering-verification.json)

Earlier fixture setup exposed Windows path-length limits; test roots were
shortened within the permitted workspace. No sandbox policy was bypassed.
The local Python installation also emitted a console-finalizer error after
pytest exited; this is recorded separately from the successful test results.

## Follow-up after the 10:53 operator approval

One native read-only `az cloud show` retry failed before returning cloud data:
`PermissionError` writing `C:\Users\alanjiao\.azure\az.sess`. Actual sandbox
access therefore remains blocked despite the renewed conversational approval.
No alternative config directory, copied identity cache or token extraction
was used.

The authenticated portal provided further evidence. In the selected subscription,
the agent set region `chinaeast3` and opened the VM size picker. The exact
`NC96ads_A100_v4` row appeared with 96 vCPUs and 880 GiB RAM. The price cell
displayed unavailable and the row did not become selected; the selection button
remained disabled. The same occurred after changing the undeployed draft from
Trusted Launch / Ubuntu 24.04 Gen2 to Standard / Ubuntu 22.04 Gen2.

See [the bounded UI observation](../runs/operations/20260916-ce3-restart/portal-sku-observation.json).
This is **not** proof that CE3 has no capacity, that the subscription's quota
is exhausted, or that the SKU is universally unavailable. The portal did not
identify the underlying cause. No smaller SKU, alternative subscription or
different region was substituted. No resource group or VM was created.

The agent then opened **Quotas / My quotas**. The shell and page title loaded,
but no quota values or limits were returned to the browser connection. No
quota-increase request was submitted. Resumption still needs the normal
`.azure` directory allowed by the actual Copilot sandbox policy; conversational
approval has not changed the observed filesystem denial.

## Fixed-input ARM deployment after the 11:37 request

The operator authorized another working creation method. The custom template
editor is in a cross-origin React frame, so the agent used the documented
Azure Portal URL-template loader instead of extracting tokens or bypassing
browser security. Merely renaming parameters did not disable automatic portal
VM-size/SSH-key controls. A resolved-input template then loaded successfully.

- Immutable source: `a78fd55c715bc4b355fb3ceb1d73f6eec4268357`
- Template: `infra/azure/ce3-a100x4.resolved.json`
- Target resource group: `rg-wda-ce3-20260916`
- VM: `wda-ce3-a100x4-0916`
- Subscription: `d124de35-7837-4ffe-ba2b-2ec1d31477d0`
- VM creation displayed by Azure: `2026/9/16 UTC 03:56`
- Actual SKU: `Standard_NC96ads_A100_v4`, 96 vCPUs, 880 GiB RAM
- Actual region: `chinaeast3`

The template was submitted **once**. Azure subsequently notified aggregate
deployment failure. A direct VM overview nevertheless showed the VM **Running**
and its agent **Ready**. It was not recreated. See
[the VM observation](../runs/operations/20260916-ce3-restart/vm-observation.json).

No guest private key was serialized. All inbound ports are denied in the
submitted NSG definition; administration uses Azure VM Run Command. The image
alias `latest` was used for initial infrastructure provisioning only. The
resolved exact image must still be captured before scientific environment
freezing. Neither VM creation nor a passing guest command qualifies an assay.

## Guest environment work

The first Run Command invocation returned empty stdout. Investigation showed
that filling Monaco's hidden textarea did not update its actual script model.
The agent corrected the editor through Monaco's model API, verified the actual
model SHA-256 against the recorded script, and ran the read-only inventory.
The empty invocation is not counted as a successful inventory or model trial.

[The inventory output](../runs/operations/20260916-ce3-restart/guest-environment-inventory.json)
at `2026-09-16T04:12:01.080369+00:00` shows:

- Four NVIDIA PCI devices, vendor `0x10de`, device `0x20b5`.
- 96 logical CPUs, Ubuntu Azure kernel `6.8.0-1064-azure`, Python `3.10.12`.
- `nvidia-smi` was not installed.
- IMDS returned the image alias `latest`, not a resolved exact image version.

The [bounded driver-install script](../runs/operations/20260916-ce3-restart/scripts/02-install-gpu-driver.sh)
was then submitted once. It permits at most five minutes for package-index
refresh and fifteen minutes for installation, records the resolved package
version, and logs to `/var/log/wda/gpu-driver-install-20260916.log`. It does not
download models or start an experiment.

The installer returned its explicit readiness error code 22 even though its
output listed all four GPUs. Its requested `nvidia-driver-550-server` package
was transitional and installed the 580 server driver. The cause of the first
nonzero probe was not established and was not hidden:
[installer observation](../runs/operations/20260916-ce3-restart/driver-install-observation.json).

A separate read-only query, without reinstalling, then returned **exit 0** and
confirmed **four NVIDIA A100 80GB PCIe devices, 81920 MiB each, driver 580.178.04**:
[driver readiness](../runs/operations/20260916-ce3-restart/gpu-driver-readiness.json).
This proves hardware/driver readiness, not model or J-lens qualification.

## Cost control

The submitted template requested a daily shutdown at **07:50 UTC / 15:50
Beijing time on September 16**. Because overall deployment failed, the schedule
is **not verified** and must not be relied on as a working billing cap.

A best-effort session follow-up was registered for **07:45 UTC** to inspect and
stop/deallocate only this VM unless the operator explicitly extends runtime.
This depends on the app and control-plane access remaining available; it is
not an Azure-enforced limit. If preparation remains blocked, stop/deallocate
earlier and verify `Stopped (deallocated)`. Guest shutdown alone does not stop
allocated-VM charges. Disks and the static public IP can continue to incur
charges after compute deallocation.

Following the driver check, the agent requested Stop and confirmed the portal's
explicit **deallocation** dialog for this exact VM. No VM, disk or IP was
deleted. At **2026-09-16T04:32:32.890Z** the portal status was independently read
as **Stopped (deallocated)**. See
[the deallocation observation](../runs/operations/20260916-ce3-restart/deallocation-observation.json).
Compute allocation charges are stopped; historical charges and ongoing
disk/static-IP charges still require reconciliation.

The 07:45 UTC session follow-up was cleared after verified early deallocation.
No automatic Azure shutdown success is claimed, and no model trial was started
just to keep the VM busy. Hardware readiness is complete; the real-model
instrument qualification and new scientific seal remain outstanding.
